# Access code for the /vault gate — entering this redirects to the private /cv page
ACCESS_CODE=8888

# === AI Audio Guide — same providers/keys as the original setup ===
# TTS is served by Fish Audio (the working path; the old OpenRouter /audio/speech
# route was invalid). Paste your key below.
FISH_API_KEY=
FISH_MODEL=s2.1-pro
FISH_REFERENCE_ID=

# LLM for optional translation (OpenRouter / Gemini) — used only with --translate.
OPENROUTER_API_KEY=
OPENROUTER_API_KEYS=
OPENROUTER_MODEL=google/gemma-4-26b-a4b-it:free
OPENROUTER_API_URL=https://openrouter.ai/api/v1
GEMINI_API_KEYS=


# Admin password for /settings backend (no hardcoded fallback in source)
SETTINGS_PASSWORD=ldOVCXY99lrR2pk6e6rCtXC
