# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# local request data (access requests)
/.data

# local snapshots / backups (not for deploy)
/shakya-work-08-28-reverted.zip
/shakya-work-seo-fixed.zip

# agent working backups (contain deleted-source snapshots)
/.workbuddy/backup/

# typescript
*.tsbuildinfo
next-env.d.ts

# deploy prompt embeds live credentials — keep out of the repo
/HERMES-DEPLOY-PROMPT.md
